export type Form = {
    finca: string
    bloque: string
    cama: string
    estado: string
    cantidad: string
}

export type Observation = {
    fecha: string
    finca: string
    bloque: string
    cama: string
    estado: string
    cantidad: number
}
